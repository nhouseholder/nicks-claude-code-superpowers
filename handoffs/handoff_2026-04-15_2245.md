# Handoff — superpowers — 2026-04-15 22:45
**Repo:** nhouseholder/nicks-claude-code-superpowers | **Branch:** main | **Last commit:** 9b155f0 add: sonnet-switch-gate Stop hook (2026-04-15)
**Local = Remote:** yes

---

## 1. Session Summary
Deep debug + rebuild of the communication/plan enforcement hook stack. Diagnosed multiple silent failures (Stop hook was a complete no-op for weeks due to JSONL parsing bug), ripped out DONE/FOUND debrief system per user frustration, rebuilt Sonnet-switch enforcement as a narrow single-purpose hook, and fixed worktree-blind CWD logic in bash-guard. Also removed `response-format-guard.py` from Stop hooks entirely — user wants natural exec-brief summaries with no format policing.

## 2. What Changed
- **sonnet-switch-gate.py (NEW)**: Narrow Stop hook — blocks response only when ACTIVE_PLAN is fresh (<5 min) and hand-off line is missing. Max 2 blocks per session. No BLUF/format/debrief rules.
- **response-format-guard.py REMOVED from Stop hooks**: Was causing re-prompt storms + duplicate summaries. JSONL parsing bug meant it was silently broken anyway.
- **bash-guard.py `effective_cwd()`**: Extracts CWD from `cd <path> &&` prefix so deploys/pushes from worktrees no longer block when commands cd to canonical path.
- **response-format-guard.py JSONL fix (then retired)**: Changed `json.load()` to line-by-line parse. Was caught silently for weeks — every Stop hook check was a no-op.
- **Communication rules simplified**: CLAUDE.md + `communication-rule-injector.py` no longer require `DONE:/FOUND:` labels. Natural exec-brief prose only.
- **CLAUDE.md Post-Change Report**: Dropped DONE: prefix — now opens with plain one-line summary.
- **Ultra-brief + exec-brief feedback memories** saved to project memory directory.
- **Widened Sonnet-switch age window** (while hook existed): 60s → 300s, then moved to dedicated gate.

Git diff stat (last 10 commits): 7 files changed, 400 insertions(+), 22 deletions(-)

## 3. In Progress / Blocked
**In progress:** All tasks completed. One untracked `.github/` directory remains in working tree (unrelated — not staged).
**Blocked:** Nothing blocked.

## 4. Next Steps (Prioritized)
1. **Live pipeline test** — Next session: write a plan via Opus, confirm `sonnet-switch-gate.py` blocks if hand-off line is missing; verify `plan-mode-enforcer.py` UserPromptSubmit guard still surfaces the nudge on next prompt.
2. **Verify exec-brief communication sticks** — Session injector now just says "natural summary like employee to boss." Watch for drift back into templated formats.
3. **Audit remaining hooks for worktree/CWD blindness** — unpushed-commits + bash-guard are clean; confirm plan-execution-guard, plan-mode-enforcer, and plan-write-guard-activator all route through `_plan_utils` correctly in worktrees.
4. **Decide on `.github/` directory** — untracked, unrelated to this session. Stage or gitignore next time.

## 5. Warnings
- **response-format-guard.py was silently dead for weeks** — `json.load()` crashed on JSONL, caught by bare except, returned empty string. Every debrief/format/Sonnet check ran as a no-op. Lesson: hooks with bare `except: return ""` hide real breakage. Audit others.
- **60-second ACTIVE_PLAN age window was too narrow** — any plan write + response >60s bypassed Sonnet-switch enforcement. Fixed to 300s (now lives in dedicated gate).
- **DONE/FOUND debrief was actively harmful** — caused duplicate summaries, status tables, and hook re-prompt storms. User rightly torched it.
- Clean rebuild; no commits lost; all changes synced and pushed.

## 6. For The Next Agent
Read first: (1) This handoff, (2) `~/.claude/CLAUDE.md` (simplified communication section), (3) `~/.claude/anti-patterns.md`
**Canonical path:** `/Users/nicholashouseholder/ProjectsHQ/superpowers`
Key new files: `hooks/sonnet-switch-gate.py` (single-purpose Sonnet gate), `hooks/bash-guard.py:effective_cwd()` (worktree CWD fix).
Key removed: `response-format-guard.py` registration in Stop hooks (file still exists but not invoked).
